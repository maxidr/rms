var row = $("table tr#<%= @requerimiento.id %>");
new_row = row.next('.row_details').addClass('hover');

new_row.children('td').empty().append("<b>Materiales: </b><%= @materiales.map(&:nombre).to_sentence %>");

if( row.hasClass('odd') ){  new_row.addClass("odd"); }

