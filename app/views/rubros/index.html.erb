<div class="contextual">
  <%= link_to 'Nuevo rubro', new_rubro_path %>
</div>

<h1>Rubros</h1>

<table>
	<thead>
		<tr>
		  <th>Nombre</th>
		  <th></th>
		</tr>
	</thead>
	<tbody>
	<% @rubros.each do |rubro| %>
		<tr>
		  <td><%= rubro.nombre %></td>
		  <td class="actions">
		  	<%= link_to 'Ver', rubro %>
				<%= link_to 'Editar', edit_rubro_path(rubro) %>
				<%= link_to 'Eliminar', rubro, :confirm => 'Esta seguro?', :method => :delete %>
			</td>
		</tr>
	<% end %>
	</tbody>
	<%= will_paginate @rubros %>
</table>

