<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
  	<p>
  		El requerimiento n° <%= @requerimiento.id %> que a solicitado fué rechazado por responsable del 
  		sector "<%= @requerimiento.sector.nombre %>" (<%= @requerimiento.sector.nombre_responsable %>).
  	</p>
  	<p>El motivo es:</p>
  	<p><%= @motivo %></p>
  	
  	<p>Puede acceder al requerimiento mediante el siguiente <%= link_to 'link', requerimiento_url(@requerimiento) %>.</p>
  </body>
</html>

